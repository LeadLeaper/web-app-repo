---
phase: 02-profile-system
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - phase-2/profile-panel.js
autonomous: true
requirements: [PROF-01, PROF-07]

must_haves:
  truths:
    - "User can close panel by clicking X button"
    - "User can close panel by clicking backdrop (outside panel)"
    - "User can close panel by pressing ESC key"
    - "User can navigate to previous contact via arrow button"
    - "User can navigate to next contact via arrow button"
    - "Panel content updates smoothly when switching contacts"
  artifacts:
    - path: "phase-2/profile-panel.js"
      provides: "Panel interaction handlers, close methods, prev/next navigation"
      min_lines: 250
      exports: ["closeProfilePanel", "setupPanelHandlers", "navigateContact"]
  key_links:
    - from: "phase-2/profile-panel.js"
      to: "close handlers"
      via: "click and keydown event listeners"
      pattern: "\\$\\(document\\)\\.on\\('click'|'keydown'"
    - from: "phase-2/profile-panel.js"
      to: "panel state classes"
      via: "addClass/removeClass .open and .visible"
      pattern: "addClass\\('open'|removeClass\\('open'"
---

<objective>
Implement panel interaction handlers for close methods (X button, backdrop, ESC key) and prev/next contact navigation.

Purpose: Complete the panel's interactive behavior following user requirements for multiple close methods and familiar navigation workflow
Output: Fully interactive panel with all close triggers working and prev/next navigation maintaining user context
</objective>

<execution_context>
@C:/Users/skips/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/skips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-profile-system/02-CONTEXT.md
@.planning/phases/02-profile-system/02-RESEARCH.md
@.planning/phases/02-profile-system/02-01-SUMMARY.md
@.planning/phases/02-profile-system/02-02-SUMMARY.md
@existing/nav-left.js
@phase-2/profile-panel.html
@phase-2/profile-panel.css
@phase-2/profile-panel.js

## Phase 1 Patterns to Reuse

From nav-left.js:
- jQuery event delegation: $(document).on('click', selector, handler)
- State management via CSS classes and localStorage
- Event handler setup in document ready

From RESEARCH.md Code Examples:
- Multiple close method pattern (close button, backdrop click, ESC key)
- Prevent double-open with hasClass check
- Animation timing coordination (300ms delays)
- Cross-fade content when switching contacts (150ms out, 150ms in)

## User Decisions

**Locked (MUST implement exactly as specified):**
- Close triggers: X button, click outside panel (backdrop), ESC key - all three methods MUST work
- Prev/Next navigation: Keep arrows (like current modal)
  - Users can cycle through leads without closing panel
  - Maintains familiar workflow
- Panel switching behavior: Claude's discretion - choose smoothest UX when opening different lead while panel is open
  - Research recommends: Cross-fade content (150ms fade out, re-render, 150ms fade in)
- URL/History: No URL changes
  - Panel is transient (doesn't update browser URL)
  - Back button doesn't close panel
- State persistence: No memory - always use smart defaults (handled in plan 02-02)

**Claude's Discretion:**
- Panel switching transition: Use cross-fade per research recommendation
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement panel open/close logic with three close methods</name>
  <files>phase-2/profile-panel.js</files>
  <action>
Create panel interaction handlers following research patterns and user requirements:

1. **Panel Setup Function** (setupPanelHandlers):
   - Call from $(document).ready()
   - Initialize all event handlers (close, navigation, etc.)
   - One-time setup, uses event delegation for dynamic content

2. **Open Panel Function** (openProfilePanel):
   - Already created in plan 02-02, enhance with:
   - Check if panel already open: if ($panel.hasClass('open')) → call updatePanelContent instead
   - If not open: render content, add .open class to panel, add .visible class to backdrop
   - Store current contact ID on panel: $panel.data('contact-id', contactData.id)
   - Store contact list for navigation: window.currentContactList (array of contacts)

3. **Close Panel Function** (closeProfilePanel):
   - Check if already closed: if (!$panel.hasClass('open')) return;
   - Remove .open class from panel (triggers slide-out animation)
   - Remove .visible class from backdrop (triggers fade-out)
   - Wait 300ms for animation to complete: setTimeout(() => { $panel.html(''); }, 300);
   - Clear stored contact ID: $panel.removeData('contact-id')

4. **Close Method 1 - Close Button Click**:
   ```javascript
   $(document).on('click', '.profile-panel .close-btn', function(e) {
       e.preventDefault();
       closeProfilePanel();
   });
   ```

5. **Close Method 2 - Backdrop Click**:
   ```javascript
   $(document).on('click', '.profile-panel-backdrop', function(e) {
       if (e.target === this) { // Only if clicked on backdrop, not children
           closeProfilePanel();
       }
   });
   ```

6. **Close Method 3 - ESC Key**:
   ```javascript
   $(document).on('keydown', function(e) {
       const $panel = $('#profile-panel');
       if (e.which === 27 && $panel.hasClass('open')) { // ESC key = 27
           closeProfilePanel();
       }
   });
   ```

7. **Update Panel Content** (updatePanelContent):
   - Called when opening new contact while panel already open
   - Cross-fade transition per research recommendation:
     * Fade out content: $('.profile-content').fadeOut(150)
     * After fade complete: Re-render with new contact data
     * Fade in new content: $('.profile-content').fadeIn(150)
   - Update header (photo, name, status) immediately (no fade)
   - Update stored contact ID
   - Re-initialize section toggles

All close methods MUST work per user requirement - test each thoroughly.

Follow patterns from nav-left.js and research code examples. Use jQuery event delegation for all handlers.
  </action>
  <verify>
    <automated>grep -c "closeProfilePanel" phase-2/profile-panel.js && grep -c "keydown.*27" phase-2/profile-panel.js</automated>
    Manual checks:
    - setupPanelHandlers function exists and called on document ready
    - Three close handlers implemented (close button, backdrop, ESC key)
    - closeProfilePanel removes .open and .visible classes
    - Timeout clears content after 300ms animation
    - openProfilePanel checks for already-open state
    - updatePanelContent uses cross-fade (fadeOut 150ms, fadeIn 150ms)
  </verify>
  <done>Panel open/close logic complete with three working close methods (X button, backdrop click, ESC key) and smooth content updates when switching contacts</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement prev/next contact navigation</name>
  <files>phase-2/profile-panel.js</files>
  <action>
Create contact navigation handlers to maintain familiar modal workflow per user requirement:

1. **Navigation Handler Setup**:
   ```javascript
   $(document).on('click', '.profile-panel .btn-nav', function() {
       const direction = $(this).data('nav'); // 'prev' or 'next'
       navigateContact(direction);
   });
   ```

2. **Navigate Contact Function** (navigateContact):
   - Get current contact ID from panel data: const currentId = $('#profile-panel').data('contact-id');
   - Get contact list from window state: const contacts = window.currentContactList || [];
   - Find current index: const currentIndex = contacts.findIndex(c => c.id === currentId);
   - If not found: return; (safety check)
   - Calculate next index based on direction:
     * if (direction === 'next'): nextIndex = (currentIndex + 1) % contacts.length; (wrap around)
     * else: nextIndex = (currentIndex - 1 + contacts.length) % contacts.length; (wrap around)
   - Get next contact: const nextContact = contacts[nextIndex];
   - Update panel: updatePanelContent(nextContact);

3. **Contact List Management**:
   - When opening panel from list view, caller must set: window.currentContactList = [array of contacts]
   - When opening panel from other context (search, etc.), set appropriate list
   - This allows prev/next to work from any entry point

4. **Visual Feedback**:
   - During navigation, could show brief loading state (optional enhancement)
   - Cross-fade happens automatically via updatePanelContent

5. **Edge Cases to Handle**:
   - Empty contact list: Disable prev/next buttons or hide them
   - Single contact: Prev/next cycle back to same contact (wrap around)
   - Contact list changes while panel open: Use current list state (acceptable per user requirements)

Follow patterns from research code examples. Maintain familiar workflow from existing modal navigation.

Test navigation thoroughly: First contact → next → next → wrap to first. Last contact → prev → prev → wrap to last.
  </action>
  <verify>
    <automated>grep -c "navigateContact" phase-2/profile-panel.js && grep -c "currentContactList" phase-2/profile-panel.js</automated>
    Manual checks:
    - navigateContact function exists and accepts direction parameter
    - Wrap-around logic implemented (modulo calculation)
    - Prev button navigates to previous contact
    - Next button navigates to next contact
    - Edge cases handled (empty list, single contact)
    - updatePanelContent called to refresh display
  </verify>
  <done>Prev/next navigation complete with wrap-around behavior, contact list management, and smooth content updates maintaining familiar modal workflow</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. **Test Close Methods:**
   - Click X button → panel should close
   - Click outside panel (on backdrop) → panel should close
   - Press ESC key → panel should close
   - All three methods should trigger same smooth close animation
2. **Test Navigation:**
   - Set window.currentContactList = [sample contacts]
   - Open panel with first contact
   - Click next arrow → should show second contact with cross-fade
   - Click next repeatedly → should wrap to first contact
   - Click prev arrow → should show previous contact
   - Click prev from first → should wrap to last contact
3. **Test Panel Switching:**
   - Open panel with contact A
   - Open panel with contact B (simulate clicking different row)
   - Should cross-fade content smoothly (150ms out, 150ms in)
4. **Test Edge Cases:**
   - Try closing panel that's already closed (should do nothing)
   - Try opening panel that's already open (should update content)
   - Navigation with single contact in list (should work but stay same)
5. Test in both 1920x1080 and 1366x768 viewports
</verification>

<success_criteria>
- X button closes panel with smooth slide-out animation
- Backdrop click closes panel (only when clicking backdrop itself, not children)
- ESC key closes panel when panel is open
- All three close methods work reliably and trigger same animation
- Prev arrow navigates to previous contact with cross-fade transition
- Next arrow navigates to next contact with cross-fade transition
- Navigation wraps around at list boundaries (first ↔ last)
- Panel content updates smoothly when switching contacts (150ms fade out/in per research)
- Opening new contact while panel already open updates content without full close/reopen
- No URL changes when opening/closing/navigating (panel is transient per user requirement)
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-system/02-03-SUMMARY.md`
</output>
