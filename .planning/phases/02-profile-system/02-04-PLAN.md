---
phase: 02-profile-system
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - existing/webapp.html
  - existing/webapp.js
  - phase-2/integration-test.html
autonomous: false
requirements: [PROF-08]

must_haves:
  truths:
    - "Clicking contact/lead in any section opens profile panel (not modal)"
    - "No MagnificPopup modal behavior remains for profiles"
    - "All existing functionality works without breaking changes"
    - "User can perform all current tasks without retraining"
  artifacts:
    - path: "existing/webapp.js"
      provides: "Profile panel integration, modal replacement, trigger updates"
      contains: "openProfilePanel"
    - path: "existing/webapp.html"
      provides: "Profile panel integrated into main application"
      contains: "profile-panel"
    - path: "phase-2/integration-test.html"
      provides: "Integration test page with sample contact list"
      contains: "profile-panel"
  key_links:
    - from: "existing/webapp.js"
      to: "profile panel opening"
      via: "click handlers call openProfilePanel"
      pattern: "openProfilePanel\\("
    - from: "existing/webapp.html"
      to: "profile panel inclusion"
      via: "script and style tags load panel assets"
      pattern: "profile-panel\\.(js|css)"
---

<objective>
Integrate profile panel into main application, replace all modal popup triggers with panel opening, and verify no breaking changes.

Purpose: Complete the transition from modal popups to profile panel across all application sections
Output: Fully integrated profile panel replacing modals, comprehensive integration test, and verification that all existing functionality works
</objective>

<execution_context>
@C:/Users/skips/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/skips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-profile-system/02-CONTEXT.md
@.planning/phases/02-profile-system/02-RESEARCH.md
@.planning/phases/02-profile-system/02-01-SUMMARY.md
@.planning/phases/02-profile-system/02-02-SUMMARY.md
@.planning/phases/02-profile-system/02-03-SUMMARY.md
@existing/webapp.html
@existing/webapp.js
@phase-2/integration-test.html

## Integration Points from Research

Research identified these modal trigger points to replace:
- Table row clicks (main list views)
- Search results (if applicable)
- Email chain contact links
- Any other profile access points

Need to:
1. Find all MagnificPopup calls or modal open triggers
2. Replace with openProfilePanel(contactData) calls
3. Search codebase for: .magnificPopup, .modal, pmp function (popup management)

## User Decisions

**Locked (MUST implement exactly as specified):**
- Profile panel replaces existing popup behavior (no modal popups)
- All existing functionality must work without breaking changes
- User workflows remain unchanged (no retraining needed)

**Testing Requirements:**
- Verify profile access from all sections (Lists, Engagement, Dashboard, etc.)
- Verify no modal popups appear for profiles
- Verify all existing functionality intact
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Integrate profile panel into webapp and replace modal triggers</name>
  <files>existing/webapp.html, existing/webapp.js</files>
  <action>
Integrate the profile panel into the main application and replace modal popup triggers:

1. **webapp.html Integration:**
   - Add profile panel HTML at end of body (before closing body tag):
     ```html
     <!-- Profile Panel -->
     <div class="profile-panel-backdrop"></div>
     <div id="profile-panel" class="profile-panel" role="dialog" aria-hidden="true">
       <!-- Panel content will be dynamically populated -->
     </div>
     ```
   - Add CSS link in head section: `<link rel="stylesheet" href="profile-panel.css">`
   - Add JS script before closing body tag: `<script src="profile-panel.js"></script>`
   - Ensure scripts load in correct order (jQuery first, then profile-panel.js)

2. **Find and Replace Modal Triggers in webapp.js:**
   - Search for MagnificPopup calls: `.magnificPopup(`, `$.magnificPopup.open`
   - Search for modal open calls: `.modal('show')`, Bootstrap modal API calls
   - Search for profile popup function: `pmp(` or similar (profile modal popup)
   - Identify all click handlers that open profile modals

3. **Replace Modal Calls with Panel Calls:**
   - Replace: `$.magnificPopup.open({ ... })` or `.modal('show')`
   - With: `openProfilePanel(contactData)`
   - Ensure contactData object contains all required fields:
     * id, name, photo, company, title, isLive
     * details, companyInfo, socialLinks
     * notes, calls, meetings, reminders
     * emailReplies, emailLinksViewed, emailsSent

4. **Update Table Row Click Handlers:**
   - Common pattern: `$(document).on('click', '.contact-row', function() { ... });`
   - Extract contact data from table row (data attributes or hidden fields)
   - Build contactData object from row data
   - Call openProfilePanel(contactData)
   - Set window.currentContactList from table rows for prev/next navigation

5. **Remove or Comment Out MagnificPopup Setup:**
   - If MagnificPopup is only used for profiles, can remove entirely
   - If used for other modals (confirmations, etc.), leave those intact
   - Only remove profile-specific modal code

6. **Preserve Existing Functionality:**
   - Don't modify: Navigation, filtering, sorting, search, other modals
   - Only change: Profile viewing triggers
   - Test each section after changes to ensure no breakage

Follow existing jQuery patterns in webapp.js. Use data attributes or DOM traversal to extract contact info from clicked elements.

Per user requirement: All existing functionality must work without breaking changes. Test thoroughly.
  </action>
  <verify>
    <automated>grep -c "openProfilePanel" existing/webapp.js && grep -c "profile-panel" existing/webapp.html</automated>
    Manual checks:
    - Profile panel HTML added to webapp.html
    - CSS and JS links added to webapp.html
    - Modal triggers replaced with openProfilePanel calls
    - Contact data extraction logic implemented
    - window.currentContactList set for navigation
    - MagnificPopup profile code removed or commented out
  </verify>
  <done>Profile panel integrated into webapp.html, all modal triggers replaced with panel opening calls, existing functionality preserved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete profile panel system integrated into the main application:
- Profile panel slides in from right when clicking contact/lead anywhere in application
- Panel displays all contact information in organized sections (Details, Company, Links, Notes, Calls, Meetings, Reminders, Engagement)
- Panel can be closed via X button, clicking outside, or pressing ESC key
- Users can navigate between contacts using prev/next arrows
- No modal popups remain for profiles (complete replacement)
  </what-built>
  <how-to-verify>
**1. Open Integration Test Page:**
   - Open phase-2/integration-test.html in browser at 1920x1080 viewport
   - Should see left navigation (from Phase 1) and main content area

**2. Test Profile Panel Opening:**
   - Create or find a sample contact list in the test page
   - Click on a contact/lead entry
   - Expected: Profile panel slides in from right (300ms smooth animation)
   - Expected: Backdrop appears with semi-transparent overlay
   - Expected: No modal popup appears

**3. Test Panel Content Display:**
   - Verify fixed header shows: Contact photo, name, LIVE badge, status
   - Verify action buttons visible: Prev/Next arrows, Email, Call, More
   - Verify scrollable content shows sections in order:
     * Static Info: Details (expanded), Company Info (expanded), Links
     * Activity: Notes, Calls, Meetings, Reminders
     * Engagement: Email Replies, Links Viewed, Emails Sent
   - Verify populated sections are expanded
   - Verify empty sections are collapsed with "Add" prompts (Add Note, Log Call, etc.)

**4. Test Section Interaction:**
   - Click a section header → should expand/collapse smoothly (300ms slideToggle)
   - Toggle icon should rotate when expanding/collapsing
   - Verify multiple sections can be toggled independently

**5. Test Close Methods (ALL THREE MUST WORK):**
   - Click X button in top-right → panel should slide out, backdrop fade out
   - Open panel again, click on backdrop (gray area outside panel) → should close
   - Open panel again, press ESC key → should close
   - All three methods should produce identical smooth close animation

**6. Test Prev/Next Navigation:**
   - Open panel with first contact in list
   - Click Next arrow → should show next contact with cross-fade transition
   - Click Next repeatedly → should eventually wrap to first contact
   - Click Prev arrow → should show previous contact
   - Click Prev from first contact → should wrap to last contact
   - Verify content updates smoothly (150ms fade out, 150ms fade in)

**7. Test Panel Overlay Behavior:**
   - Verify panel slides OVER main content (content doesn't shift right)
   - Verify backdrop blocks interaction with main content
   - Verify header stays fixed when scrolling panel content
   - Verify action buttons stay fixed below header

**8. Test Responsive Behavior:**
   - Resize to 1366x768 (laptop viewport)
   - Verify panel still works correctly
   - Verify all functionality intact at smaller viewport

**9. Test from Different Sections:**
   - If integration-test.html has multiple sections (Lists, Dashboard, etc.)
   - Open profile from each section → should work identically
   - Verify NO modal popups appear anywhere for profiles

**10. Test Existing Functionality (No Breaking Changes):**
   - Test navigation between sections (left nav should still work)
   - Test any filters, search, sorting in list views
   - Test other modals if present (confirmations, alerts) → should still work
   - Verify all existing features function normally

**Expected Results:**
- ✅ Profile panel opens from any section with smooth slide animation
- ✅ All content sections display correctly with smart expansion defaults
- ✅ All three close methods work (X, backdrop, ESC)
- ✅ Prev/next navigation works with wrap-around
- ✅ Panel overlays content without layout shifts
- ✅ No modal popups appear for profiles
- ✅ All existing functionality works without changes
- ✅ Works correctly in both desktop (1920+) and laptop (1366-1920) viewports

**If Issues Found:**
Describe the specific problem, which test step failed, and what unexpected behavior occurred.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Comprehensive testing checklist (covered in checkpoint task):
1. Panel opens from all entry points
2. Content displays correctly with smart defaults
3. Three close methods work
4. Prev/next navigation works with wrap-around
5. Panel overlay behavior correct (no layout shifts)
6. Responsive behavior correct (1920+ and 1366-1920)
7. No modal popups remain
8. Existing functionality intact
9. Cross-browser testing (Chrome, Firefox, Safari on desktop)
</verification>

<success_criteria>
- Profile panel HTML and assets integrated into webapp.html
- All modal popup triggers replaced with openProfilePanel() calls
- Contact data extraction logic implemented for all trigger points
- window.currentContactList set correctly for navigation
- Profile panel opens when clicking contact/lead in any section
- No MagnificPopup or modal behavior remains for profiles (PROF-08)
- All existing functionality works without breaking changes (COMPAT-01)
- User can perform all current tasks without retraining (COMPAT-03)
- Panel works in desktop (1920+) and laptop (1366-1920) viewports
- Comprehensive integration test page demonstrates all functionality
- Human verification confirms all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-system/02-04-SUMMARY.md`
</output>
