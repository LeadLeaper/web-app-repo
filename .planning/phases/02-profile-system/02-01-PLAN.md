---
phase: 02-profile-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - existing/profile-panel.html
  - existing/profile-panel.css
autonomous: true
requirements: [PROF-01, PROF-07]

must_haves:
  truths:
    - "Panel slides in from right when user clicks contact/lead"
    - "Panel slides out when user clicks close button, backdrop, or ESC key"
    - "Panel overlays main content without shifting layout"
  artifacts:
    - path: "existing/profile-panel.html"
      provides: "Panel HTML structure with header, actions, scrollable content area"
      min_lines: 60
    - path: "existing/profile-panel.css"
      provides: "Overlay positioning, slide animations, backdrop, z-index management"
      min_lines: 100
      contains: "transform: translateX"
  key_links:
    - from: "existing/profile-panel.css"
      to: "CSS transitions"
      via: "transform and opacity properties"
      pattern: "transition.*300ms"
    - from: "existing/profile-panel.html"
      to: "fixed header and scrollable content"
      via: "position: sticky on header"
      pattern: "profile-header|profile-content"
---

<objective>
Create the right-hand profile panel overlay with smooth slide animations, fixed header structure, and multiple close methods.

Purpose: Establish the core panel foundation that replaces modal popups with a non-disruptive overlay pattern
Output: Working panel structure that slides in/out smoothly with proper z-index layering and overlay behavior
</objective>

<execution_context>
@C:/Users/skips/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/skips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profile-system/02-CONTEXT.md
@.planning/phases/02-profile-system/02-RESEARCH.md
@existing/nav-left.css
@existing/theme.css

## Phase 1 Patterns to Mirror

From nav-left.css (smooth animations established in Phase 1):
- CSS transforms for hardware-accelerated sliding
- 300ms transition timing (established standard)
- Fixed positioning with z-index strategy
- CSS state classes for behavior control

From theme.css (design tokens):
- Neutral color palette variables
- Shadow and spacing tokens
- Modern professional aesthetic

## User Decisions

**Locked (MUST implement exactly as specified):**
- Display mode: Overlay (panel slides over main content, doesn't push or shrink content area)
- Close triggers: X button, click outside panel (backdrop), ESC key - all three methods MUST work
- Fixed header: Contact name & photo, LIVE badge, status indicators - always visible at top while scrolling
- Action buttons: Fixed at top, below header (NOT in scrollable header area)
- Animation timing: Match left navigation patterns (300ms per research)

**Claude's Discretion:**
- Panel width (research recommends 380px - typical for SaaS panels)
- Panel switching transition (cross-fade recommended in research)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create panel HTML structure with fixed header and scrollable content</name>
  <files>existing/profile-panel.html</files>
  <action>
Create the HTML structure for the right-hand profile panel with three main sections:

1. **Fixed Header Section** (always visible):
   - Close button (×) in top-right corner
   - Contact photo (circular, placeholder image)
   - Contact name (h2)
   - LIVE badge indicator
   - Status indicators (company, title)

2. **Fixed Actions Section** (below header, above scrollable content):
   - Previous/Next navigation arrows (←/→)
   - Email button
   - Call button
   - More actions button (⋮) for dropdown

3. **Scrollable Content Section** (fills remaining height):
   - Empty container (profile-content) that will be populated dynamically
   - This section will hold collapsible sections created in plan 02-02

Use semantic HTML with clear class names:
- .profile-panel (main container)
- .profile-header (fixed header)
- .profile-actions (fixed action buttons)
- .profile-content (scrollable area)
- .profile-panel-backdrop (overlay backdrop)

Include accessibility attributes: aria-label on close button, role="dialog" on panel, aria-hidden states.

Match existing codebase patterns: Use Bootstrap 3 button styles, integrate theme.css variables for colors.
  </action>
  <verify>
    <automated>grep -c "profile-panel" existing/profile-panel.html</automated>
    Manual checks:
    - HTML contains role="dialog" on panel container
    - Close button has aria-label
    - Three main sections present (header, actions, content)
    - Backdrop element exists
  </verify>
  <done>Panel HTML structure complete with semantic sections, accessibility attributes, and placeholder content ready for dynamic population</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement CSS overlay positioning, slide animations, and z-index strategy</name>
  <files>existing/profile-panel.css</files>
  <action>
Create comprehensive styling for the profile panel following Phase 1 animation patterns:

1. **Panel Positioning** (overlay pattern per user requirement):
   - position: fixed; top: 0; right: 0; height: 100vh
   - width: 380px (optimal for content per research recommendation)
   - background-color: white
   - z-index: 1100 (above main content at 1000, below alerts/modals at 1200)
   - transform: translateX(100%) (hidden off-screen by default)
   - transition: transform 300ms ease-out (matches nav-left.css timing)

2. **Panel Open State**:
   - .profile-panel.open { transform: translateX(0); } (slides into view)

3. **Backdrop Overlay**:
   - position: fixed; top: 0; left: 0; right: 0; bottom: 0
   - background-color: rgba(0, 0, 0, 0.3) (semi-transparent black)
   - z-index: 1090 (below panel, above main content)
   - opacity: 0; pointer-events: none (hidden by default)
   - transition: opacity 300ms ease-out
   - .profile-panel-backdrop.visible { opacity: 1; pointer-events: auto; }

4. **Fixed Header Styling**:
   - position: sticky; top: 0; z-index: 10 (stays at top while content scrolls)
   - Use theme.css variables for colors and spacing
   - Contact photo: 48px circular with border
   - LIVE badge: small pill with brand color background

5. **Fixed Actions Section**:
   - position: sticky; top: [header-height]; (below header)
   - Flexbox layout for button arrangement
   - Use existing button styles from webapp.css/Bootstrap

6. **Scrollable Content Area**:
   - overflow-y: auto (enables scrolling)
   - max-height: calc(100vh - [header+actions height])
   - Smooth scrolling behavior

7. **Responsive Adjustments**:
   - @media (min-width: 1366px) and (max-width: 1919px): Reduce panel width slightly if needed
   - Ensure panel works on both desktop (1920+) and laptop (1366-1920) viewports

Use CSS custom properties from theme.css for all colors, shadows, and spacing. Mirror the professional aesthetic from nav-left.css.
  </action>
  <verify>
    <automated>grep -c "transform: translateX" existing/profile-panel.css && grep -c "transition.*300ms" existing/profile-panel.css</automated>
    Manual checks:
    - Panel starts off-screen (translateX(100%))
    - .open class slides panel into view
    - Backdrop has opacity transition
    - Z-index values documented and follow strategy (1090 backdrop, 1100 panel)
    - Header is position: sticky
    - Content area has overflow-y: auto
  </verify>
  <done>Panel CSS complete with overlay positioning, smooth 300ms slide animations, backdrop transitions, fixed header/actions, scrollable content, and documented z-index strategy</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Open integration-test.html and add link to load profile-panel.html
2. Manually add .open class to panel in DevTools - should slide in from right
3. Remove .open class - should slide out
4. Verify backdrop opacity transitions when .visible class toggled
5. Verify panel overlays main content without shifting layout
6. Verify header stays fixed when scrolling content
7. Test in both 1920x1080 and 1366x768 viewports
</verification>

<success_criteria>
- Profile panel HTML structure exists with header, actions, and content sections
- Panel slides smoothly in 300ms from right edge when .open class added
- Backdrop fades in/out with .visible class
- Panel overlays content (no layout shift in main content area)
- Fixed header remains visible when scrolling
- Z-index strategy prevents conflicts with nav (1000) and future modals (1200)
- All CSS uses theme.css variables for consistency
- Works correctly in desktop (1920+) and laptop (1366-1920) viewports
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-system/02-01-SUMMARY.md`
</output>
