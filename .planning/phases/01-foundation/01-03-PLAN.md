---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - existing/webapp.html
  - existing/webapp.css
  - existing/integration-test.html
autonomous: false
requirements:
  - COMPAT-01
  - COMPAT-03

must_haves:
  truths:
    - "Left navigation replaces top horizontal navbar in main application"
    - "Main content area adjusts layout when nav expands/collapses"
    - "All 9 sections are accessible via new navigation"
    - "New theme and typography are applied throughout existing application"
    - "All existing functionality works without breaking changes"
    - "Layout works correctly in desktop (1920+) and laptop (1366-1920) viewports"
  artifacts:
    - path: "existing/webapp.html"
      provides: "Updated main application HTML with left nav integration"
      contains: "nav-left.html"
    - path: "existing/webapp.css"
      provides: "Updated layout styles for left-nav pattern"
      contains: "main-content"
    - path: "existing/integration-test.html"
      provides: "Test page demonstrating all 9 sections with new navigation"
      min_lines: 50
  key_links:
    - from: "existing/webapp.html"
      to: "existing/nav-left.html"
      via: "HTML include or inline embed"
      pattern: "nav-left\\.html|id=\"left-nav\""
    - from: "existing/webapp.html"
      to: "existing/theme.css"
      via: "CSS link tag"
      pattern: "<link.*theme\\.css"
    - from: "existing/webapp.html"
      to: "existing/typography.css"
      via: "CSS link tag"
      pattern: "<link.*typography\\.css"
    - from: "existing/webapp.css"
      to: ".main-content layout"
      via: "Flexbox margin adjustment"
      pattern: "margin-left.*250px|margin-left.*60px"
---

<objective>
Integrate left navigation and modern theme into existing application, replacing horizontal navbar while preserving all functionality.

Purpose: Connect the foundational navigation and visual design to the live application, ensuring compatibility and consistent user experience across all sections.
Output: Fully integrated application with left navigation, modern theme, responsive layout, and verified compatibility across all 9 sections.
</objective>

<execution_context>
@C:/Users/skips/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/skips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

Integration must work with existing patterns:
- Existing .navbar-fixed-top class (to be replaced)
- Bootstrap 2023 grid layout
- jQuery 2023 for DOM manipulation
- Body class system for feature flags
- Existing main-content structure
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace horizontal navbar with left navigation and update layout</name>
  <files>existing/webapp.html, existing/webapp.css</files>
  <action>
Update webapp.html to integrate left navigation:
- Remove or comment out existing .navbar-fixed-top horizontal navigation
- Include/embed nav-left.html content at start of body
- Add CSS link tags for theme.css and typography.css in head
- Add script tag for nav-left.js before closing body tag
- Ensure proper load order: theme.css → typography.css → nav-left.css → existing webapp.css (for override capability)

Update webapp.css to accommodate left navigation layout:
- Adjust .main-content margin-left to account for nav panel:
  - When nav collapsed (.nav-collapsed): margin-left: 60px
  - When nav pinned (.nav-pinned): margin-left: 250px
  - Use CSS transitions for smooth layout shift: transition: margin-left 0.3s ease-in-out
- Remove or update .navbar-fixed-top styles (no longer needed)
- Ensure #page-inner-content and .container adjust correctly with new left margin
- Add flexbox layout to body: display: flex to accommodate fixed left nav + flexible content area

**Responsive adjustments:**
- Desktop (1920+): Full 250px nav width when pinned
- Laptop (1366-1920): Same 250px width (content area adapts via flexbox)
- Use @media queries to adjust spacing if needed

**Z-index management:**
- Left nav: z-index: 1000
- Main content: z-index: 1
- Ensure dropdowns/modals still appear above nav (z-index: 1050+)

**Compatibility preservation:**
- Keep all existing jQuery handlers intact
- Preserve all existing body classes (acctMgr, engSel, etc.)
- Don't remove existing JavaScript functionality
- Only modify layout-related CSS, not functional styles
  </action>
  <verify>
    <automated>
# Verify integration completed
test -f existing/webapp.html
grep -q 'theme.css' existing/webapp.html
grep -q 'typography.css' existing/webapp.html
grep -q 'nav-left' existing/webapp.html
test -f existing/webapp.css
grep -q 'margin-left.*60px\|margin-left.*250px' existing/webapp.css
    </automated>
  </verify>
  <done>Horizontal navbar removed, left navigation integrated into webapp.html, layout adjusted with responsive margins, CSS includes proper load order, all existing functionality preserved</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive integration test for all 9 sections</name>
  <files>existing/integration-test.html</files>
  <action>
Create integration-test.html to verify navigation and theme across all sections:

**Structure:**
- Full page layout including left nav, theme, and typography
- Switchable content areas representing all 9 sections:
  1. Team (management dashboard preview)
  2. Dashboard (user dashboard preview)
  3. Lists (user-created lists preview)
  4. Engagement (email templates preview)
  5. Tracking (email opens preview)
  6. Alerts (issues preview)
  7. User (profile management preview)
  8. Integrations (third-party connections preview)
  9. Help (support preview)

**Content for each section:**
- Section heading using new typography
- Sample data table or card layout using new theme colors
- Interactive elements (buttons, links) with hover states
- Use actual UI patterns from existing application (tables, panels, buttons)

**Test scenarios embedded:**
- Nav expand/collapse on hover
- Nav pin/unpin button
- Active section highlighting
- Color palette visibility (backgrounds, borders, text)
- Typography rendering at different sizes
- Responsive behavior: include viewport resize instructions

**Testing checklist in HTML comments:**
```html
<!--
INTEGRATION TEST CHECKLIST:
[ ] Left nav appears at 60px width on load
[ ] Hovering expands nav to 250px
[ ] Pin button locks nav open
[ ] localStorage persists nav state on reload
[ ] All 9 section icons are visible
[ ] Clicking each nav item shows corresponding section content
[ ] Active section is highlighted with brand blue
[ ] Color palette renders correctly (neutrals, brand, semantic)
[ ] Typography is crisp and readable
[ ] Responsive layout works at 1920x1080
[ ] Responsive layout works at 1366x768
[ ] No console errors
[ ] All existing jQuery functionality works
-->
```

Include inline JavaScript to simulate section switching without full page reload (for testing purposes).
  </action>
  <verify>
    <automated>
# Verify test file created with all sections
test -f existing/integration-test.html
grep -q 'Team' existing/integration-test.html
grep -q 'Dashboard' existing/integration-test.html
grep -q 'Lists' existing/integration-test.html
grep -q 'Engagement' existing/integration-test.html
grep -q 'Tracking' existing/integration-test.html
grep -q 'Alerts' existing/integration-test.html
grep -q 'User' existing/integration-test.html
grep -q 'Integrations' existing/integration-test.html
grep -q 'Help' existing/integration-test.html
    </automated>
  </verify>
  <done>Integration test page created with all 9 sections, testing checklist, and interactive section switching</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 foundation: left navigation with expand/collapse behavior, modern visual theme with neutral palette and typography, integrated into existing application layout, and comprehensive compatibility testing across all 9 sections.
  </what-built>
  <how-to-verify>
**Open integration-test.html in browser:**

1. **Navigation behavior (1920x1080 viewport):**
   - Verify nav panel appears on left at 60px width
   - Hover over nav → should expand to 250px smoothly
   - Move mouse away → should collapse back to 60px
   - Click pin button → nav should stay expanded
   - Reload page → nav should remain pinned (localStorage check)
   - Click unpin → nav should collapse to 60px

2. **Visual design:**
   - Verify all 9 section icons are visible and recognizable
   - Verify neutral color palette: grays for backgrounds, borders, text
   - Verify brand blue (#08c) used for active section highlight
   - Verify typography is modern and readable (system fonts)
   - Verify smooth transitions (200-300ms) on nav expand/collapse

3. **Section navigation:**
   - Click each of the 9 nav items (Team, Dashboard, Lists, Engagement, Tracking, Alerts, User, Integrations, Help)
   - Verify corresponding section content appears
   - Verify active section is highlighted in nav
   - Verify main content area adjusts margin when nav expands/collapses

4. **Responsive behavior (1366x768 viewport):**
   - Resize browser to 1366x768
   - Verify nav still works correctly (60px collapsed, 250px expanded)
   - Verify font sizes scale down slightly (~10% reduction)
   - Verify layout remains intact without horizontal scrolling

5. **Compatibility:**
   - Open browser console → verify no JavaScript errors
   - Test any existing interactive elements (buttons, dropdowns) → should work as before
   - Verify localStorage in dev tools → should show 'navPinned' key
   - Test with nav collapsed → verify content area uses full width minus 60px
   - Test with nav pinned → verify content area uses full width minus 250px

**Expected outcomes:**
- Navigation is smooth, responsive, and persists state
- Visual design is modern, professional, and consistent
- All 9 sections are accessible and functional
- No breaking changes to existing functionality
- Works correctly in both 1920+ and 1366-1920 viewports

**If any issues found:**
- Document specific problems (which section, which viewport, what breaks)
- Note console errors if present
- Describe expected vs actual behavior
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass, or describe specific issues that need fixing.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete verification:
1. All 11 requirement IDs addressed across 3 plans
2. Left navigation fully functional with expand/collapse
3. Modern theme and typography applied consistently
4. Integration preserves all existing functionality
5. Responsive layout works for desktop and laptop viewports
6. No breaking changes introduced
</verification>

<success_criteria>
- Left navigation replaces horizontal navbar in main application
- All 9 sections accessible via new left-side navigation
- Modern neutral color palette visible throughout
- Typography is professional and scales responsively
- Nav state persists via localStorage across page loads
- Main content layout adjusts smoothly when nav expands/collapses
- Integration test demonstrates all functionality working
- Works correctly in 1920+ and 1366-1920 viewports
- Zero breaking changes to existing features
- User can perform all current tasks without retraining
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
